export const calculateDays = (dateRange) => {
  // ... fungsi calculateDays
};

export const calculateDiscount = (promo, total, minTransaction) => {
  // ... fungsi calculateDiscount
};

export const calculateTotal = (params) => {
  // ... fungsi calculateTotal
}; 